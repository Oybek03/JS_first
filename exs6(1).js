let one = Number(prompt("1-sonni yozing"));
let two = Number(prompt("2-sonni yozing"));
let three = Number(prompt("3-sonni yozing"));

// function taqqoslash() {
  if (one > two) {
    console.log(`eng kattasi ${one}`);
  } else if (one < two) {
    console.log(`eng katta son ${two}`);
  } else if (one > three) {
    console.log(`eng kattasi ${one}`);
  } else if (one < three) {
    console.log(`eng kattasi ${three}`);
  } else if (two > three) {
    console.log(`eng kattasi ${two}`);
  } else if (two < three) {
    console.log(`eng kattasi ${three}`);
  }
// }

